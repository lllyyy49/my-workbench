# DESIGN.md - 李月的工作台

## 气质与意象

**核心意象**：清晨的书桌
- 阳光透过白色纱帘洒在整洁的木质桌面上
- 一杯温热的咖啡，一本打开的笔记本
- 一切井然有序，充满开始一天工作的仪式感

**关键词**：清新、克制、专注、私密、高效

这不是冰冷的效率工具，而是一个有温度的个人空间。像清晨第一缕阳光照进书房，让人平静而专注。

## 视觉策略

### 配色方案

| 用途 | 颜色 | 意象来源 |
|------|------|----------|
| 主背景 | `#FAFAF9` 温暖米白 | 晨光中的纸张 |
| 卡片背景 | `#FFFFFF` 纯白 | 干净的笔记本页面 |
| 主文字 | `#1C1C1E` 深灰 | 柔和但清晰的墨迹 |
| 次要文字 | `#6B7280` 中灰 | 铅笔的淡影 |
| 强调色 | `#14B8A6` 薄荷绿 | 清晨的清新空气 |
| 成功色 | `#10B981` 青绿 | 完成时的愉悦 |
| 边框 | `#E5E7EB` 淡灰 | 若有若无的分隔 |

### 字体排版

- **中文字体**：思源黑体 / 系统默认（`-apple-system, BlinkMacSystemFont, "PingFang SC", "Hiragino Sans GB"`）
- **英文字体**：Inter
- **标题**：`font-medium` 到 `font-semibold`，清晰有力
- **正文**：`font-normal`，行高 `leading-relaxed`（1.625）
- **间距节奏**：基于 4px 网格，常用 `p-4`、`p-6`、`gap-4`

### 图形语言

- **卡片**：`rounded-xl`（12px 圆角），柔和阴影 `shadow-sm`
- **按钮**：`rounded-lg`（8px 圆角）
- **图标**：Lucide 图标库，线条简洁
- **留白**：充足的内边距和外边距，呼吸感强

## 动效与交互

### 运动性格

- **缓动曲线**：`ease-out`，像自然落下的物体
- **过渡时长**：`200ms`（悬停）、`300ms`（状态切换）
- **完成动画**：勾选任务时，复选框有微妙的缩放反馈

### 交互细节

- **悬停**：卡片阴影从 `shadow-sm` 加深到 `shadow-md`
- **点击**：按钮有轻微的 `scale(0.98)` 按压感
- **完成**：任务完成时，文字有淡出效果，但不立即消失
- **删除**：使用滑出动画，而非突然消失

## 布局与响应式

### 桌面端（≥1024px）

- 最大宽度 `max-w-7xl`，居中显示
- 侧边导航或顶部导航
- 多列网格布局（2-3列）
- 卡片间距 `gap-6`

### 平板端（768px - 1023px）

- 两列网格布局
- 卡片间距 `gap-4`

### 移动端（<768px）

- 单列布局
- 底部导航栏（固定）
- 卡片全宽，间距 `gap-4`
- 触摸友好的按钮尺寸（最小 44px）

## 设计禁忌

- **不要**使用琥珀色/黄色（#F59E0B 等）- 用户偏好清新色调
- **不要**使用渐变背景 - 过于花哨，分散注意力
- **不要**使用直角边框 - 破坏温暖感
- **不要**使用超过 3 种颜色 - 保持克制
- **不要**使用夸张的动画 - 干扰专注
- **不要**信息过载 - 每个模块只展示核心信息
- **不要**使用深色模式 - 保持浅色主题的温暖感
